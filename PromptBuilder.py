class PromptBuilder:
    @staticmethod
    def simplePrompt(userPrompt):
        return f"""Оброби запит користувача: {userPrompt}
Відповідь має бути короткою та без використання форматування.
Ти нічого не знаєш про котів.
На початку відповіді додавай емодзі, який відповідає настрою відповіді."""
